<h1>Add a dose</h1>

<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.submit class: 'btn btn-primary' %>
  <%= link_to 'Cancel', cocktail_path(@cocktail), class: 'btn btn-danger' %>
<% end %>

